<Window Style="{StaticResource FoundryWindowStyle}" x:Class="CreatorsForge.Foundry.App.ProductSetupDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Welcome to Creators Forge Foundry" Width="760" Height="600" MinWidth="640" MinHeight="480"
    ShowInTaskbar="False" WindowStartupLocation="CenterOwner" AutomationProperties.Name="First-run setup">
  <Grid Margin="18">
    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
    <StackPanel>
      <TextBlock FontSize="24" FontWeight="SemiBold" Text="Welcome to Foundry" />
      <TextBlock Margin="0,5,0,0" Foreground="{DynamicResource MutedTextBrush}" TextWrapping="Wrap" Text="This one-time check confirms the desktop runtime and shows which optional OBS development tools are ready." />
    </StackPanel>
    <Border Grid.Row="1" Margin="0,14,0,10" Padding="10" Background="{DynamicResource ButtonBackgroundBrush}" BorderBrush="{DynamicResource PanelBorderBrush}" BorderThickness="1" CornerRadius="4">
      <TextBlock x:Name="SummaryText" FontWeight="SemiBold" TextWrapping="Wrap" />
    </Border>
    <ListBox x:Name="ChecksList" Grid.Row="2" AutomationProperties.Name="Dependency checks">
      <ListBox.ItemTemplate><DataTemplate><Grid Margin="6"><Grid.ColumnDefinitions><ColumnDefinition Width="85"/><ColumnDefinition Width="190"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0" FontWeight="SemiBold" Foreground="{DynamicResource AccentBrush}" Text="{Binding Status}"/>
        <TextBlock Grid.Column="1" FontWeight="SemiBold" Text="{Binding Check.Name}"/>
        <StackPanel Grid.Column="2"><TextBlock Text="{Binding Check.Details}" TextWrapping="Wrap"/><TextBlock Foreground="{DynamicResource MutedTextBrush}" Text="{Binding Check.Action}" TextWrapping="Wrap"/></StackPanel>
      </Grid></DataTemplate></ListBox.ItemTemplate>
    </ListBox>
    <Border Grid.Row="3" Margin="0,12" Padding="10" BorderBrush="{DynamicResource PanelBorderBrush}" BorderThickness="1" CornerRadius="4">
      <TextBlock TextWrapping="Wrap" Text="Privacy: Foundry stores projects and recovery data locally. It includes no telemetry and uploads nothing. Network access occurs only after you enable it and explicitly request an SDK download or update check." />
    </Border>
    <StackPanel Grid.Row="4" HorizontalAlignment="Right" Orientation="Horizontal">
      <Button MinWidth="150" Content="Development Toolchain..." Click="Toolchain_Click" />
      <Button MinWidth="120" Content="Finish Setup" IsDefault="True" Click="Finish_Click" />
    </StackPanel>
  </Grid>
</Window>
