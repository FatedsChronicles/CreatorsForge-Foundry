# Streamer.bot Creator Toolkit

This release-ready sample combines welcome, shout-out, and milestone actions in
one managed extension. Use Test Explorer to run all three compatibility profiles,
then build, inspect the package, complete the publishing checklist, and deploy
to a disposable Streamer.bot installation. Add the DLL compiler reference,
import the reviewed code, compile, run each action, and save the health checklist.

