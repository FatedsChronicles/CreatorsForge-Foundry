# Streamer Control Panel

Representative Phase 22C WinForms preview project. The isolated adapter renders
form chrome and recognized native-control roles from the bounded design model.
It never loads or executes the compiled managed assembly.

Open the project and press **Ctrl+Shift+P**. Change a control label or bounds in
`src/ControlPanel.cs`, save, and confirm the preview refreshes.
